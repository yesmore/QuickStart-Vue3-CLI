import { defineStore } from 'pinia'
import piniaStore from '@/store'
import { I<%= data.name %> } from './types'

export const use<%= data.name %>Store = defineStore({
  id: '<%= data.lowerName %>',
  state: () => ({

  }),
  getters: {
    
  },
  actions: {
    
  },
})

export function use<%= data.name %>OutsideStore() {
  return use<%= data.name %>Store(piniaStore)
}